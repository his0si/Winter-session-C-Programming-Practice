#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int  main(void)

{
	double w, h, area, perimeter;

	area = w * h;

	printf("사각형의 넓이:",area);
	scanf("%lf", &w, &h);

		
		
		return 0;
}