#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void)

{
	int x, y, n, i;

	printf("정수 n을 입력하시오 : ");
	scanf("%d", &n);

	for (i = 0; i <= n; i++)
	{
		srand((), TIME(NULL));

	}

	n % 2 == 0; printf("RED TEAM"); : printf("BLUE TEAM");






	return 0;
}